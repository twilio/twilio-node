/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio - Numbers
 * This is the public Twilio REST API.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import { inspect, InspectOptions } from "util";
import V3 from "../V3";
const deserialize = require("../../../base/deserialize");
const serialize = require("../../../base/serialize");
import { isValidPathParam } from "../../../base/utility";
import { ApiResponse } from "../../../base/ApiResponse";





export interface SmsVerificationSolution {
}

export interface SmsVerificationListInstance {
  _version: V3;
  _solution: SmsVerificationSolution;
  _uri: string;



  /**
   * Create a SmsVerificationInstance
   *
   * @param callback - Callback to handle processed record
   *
   * @returns Resolves to processed SmsVerificationInstance
   */
  create(callback?: (error: Error | null, item?: SmsVerificationInstance) => any): Promise<SmsVerificationInstance>

  /**
   * Create a SmsVerificationInstance and return HTTP info
   *
   * @param callback - Callback to handle processed record
   *
   * @returns Resolves to processed SmsVerificationInstance with HTTP metadata
   */
  createWithHttpInfo(callback?: (error: Error | null, item?: ApiResponse<SmsVerificationInstance>) => any): Promise<ApiResponse<SmsVerificationInstance>>



  /**
   * Provide a user-friendly representation
   */
  toJSON(): any;
  [inspect.custom](_depth: any, options: InspectOptions): any;
}

export function SmsVerificationListInstance(version: V3): SmsVerificationListInstance {
  const instance = {} as SmsVerificationListInstance;

  instance._version = version;
  instance._solution = {  };
  instance._uri = `/CallerIds/SmsVerifications`;

  instance.create = function create( callback?: (error: Error | null, items: SmsVerificationInstance) => any): Promise<SmsVerificationInstance> {
    const headers: any = {};
    headers["Accept"] = "application/json"

    let operationVersion = version,
        operationPromise = operationVersion.create({ uri: instance._uri, method: "post", headers});
    
    operationPromise = operationPromise.then(payload => new SmsVerificationInstance(operationVersion, payload));
    

    operationPromise = instance._version.setPromiseCallback(operationPromise,callback);
    return operationPromise;


    }

  instance.createWithHttpInfo = function createWithHttpInfo( callback?: (error: Error | null, items: ApiResponse<SmsVerificationInstance>) => any): Promise<ApiResponse<SmsVerificationInstance>> {
    const headers: any = {};
    headers["Accept"] = "application/json"

    let operationVersion = version;
    // CREATE, FETCH, UPDATE operations
    let operationPromise = operationVersion.createWithResponseInfo<SmsVerificationResource>({ uri: instance._uri, method: "post", headers}).then((response) : ApiResponse<SmsVerificationInstance> => ({
      ...response,
      body: new SmsVerificationInstance(operationVersion, response.body)
    }));

    operationPromise = instance._version.setPromiseCallback(operationPromise,callback);
    return operationPromise;


    }

  instance.toJSON = function toJSON() {
    return instance._solution;
  }

  instance[inspect.custom] = function inspectImpl(_depth: any, options: InspectOptions) {
    return inspect(instance.toJSON(), options);
  }

  return instance;
}



interface SmsVerificationResource {
  sid: string;
  status: string;
  phone_number: string;
}

export class SmsVerificationInstance {

  constructor(protected _version: V3, _payload: SmsVerificationResource) {
    const payload = _payload;
    this.sid = (payload.sid);
    this.status = (payload.status);
    this.phoneNumber = (payload.phone_number);

  }

  /**
   * The unique SID of this verification resource.
   */
  sid: string;
  /**
   * The status of the verification (pending, approved, etc).
   */
  status: string;
  /**
   * The phone number being verified.
   */
  phoneNumber: string;

  /**
   * Provide a user-friendly representation
   *
   * @returns Object
   */
  toJSON() {
    return {
      sid: this.sid,
      status: this.status,
      phoneNumber: this.phoneNumber,
    };
  }

  [inspect.custom](_depth: any, options: InspectOptions) {
    return inspect(this.toJSON(), options);
  }
}


