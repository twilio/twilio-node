/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio - Numbers
 * This is the public Twilio REST API.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { inspect, InspectOptions } from "util";
import V1 from "../V1";
const deserialize = require("../../../base/deserialize");
const serialize = require("../../../base/serialize");
import { isValidPathParam } from "../../../base/utility";
import { ApiResponse } from "../../../base/ApiResponse";

export interface VoiceVerificationCheckSolution {}

export interface VoiceVerificationCheckListInstance {
  _version: V1;
  _solution: VoiceVerificationCheckSolution;
  _uri: string;

  /**
   * Create a VoiceVerificationCheckInstance
   *
   * @param callback - Callback to handle processed record
   *
   * @returns Resolves to processed VoiceVerificationCheckInstance
   */
  create(
    callback?: (
      error: Error | null,
      item?: VoiceVerificationCheckInstance
    ) => any
  ): Promise<VoiceVerificationCheckInstance>;

  /**
   * Create a VoiceVerificationCheckInstance and return HTTP info
   *
   * @param callback - Callback to handle processed record
   *
   * @returns Resolves to processed VoiceVerificationCheckInstance with HTTP metadata
   */
  createWithHttpInfo(
    callback?: (
      error: Error | null,
      item?: ApiResponse<VoiceVerificationCheckInstance>
    ) => any
  ): Promise<ApiResponse<VoiceVerificationCheckInstance>>;

  /**
   * Provide a user-friendly representation
   */
  toJSON(): any;
  [inspect.custom](_depth: any, options: InspectOptions): any;
}

export function VoiceVerificationCheckListInstance(
  version: V1
): VoiceVerificationCheckListInstance {
  const instance = {} as VoiceVerificationCheckListInstance;

  instance._version = version;
  instance._solution = {};
  instance._uri = `/CallerIds/VoiceVerificationChecks`;

  instance.create = function create(
    callback?: (
      error: Error | null,
      items: VoiceVerificationCheckInstance
    ) => any
  ): Promise<VoiceVerificationCheckInstance> {
    const headers: any = {};
    headers["Accept"] = "application/json";

    let operationVersion = version,
      operationPromise = operationVersion.create({
        uri: instance._uri,
        method: "post",
        headers,
      });

    operationPromise = operationPromise.then(
      (payload) => new VoiceVerificationCheckInstance(operationVersion, payload)
    );

    operationPromise = instance._version.setPromiseCallback(
      operationPromise,
      callback
    );
    return operationPromise;
  };

  instance.createWithHttpInfo = function createWithHttpInfo(
    callback?: (
      error: Error | null,
      items: ApiResponse<VoiceVerificationCheckInstance>
    ) => any
  ): Promise<ApiResponse<VoiceVerificationCheckInstance>> {
    const headers: any = {};
    headers["Accept"] = "application/json";

    let operationVersion = version;
    // CREATE, FETCH, UPDATE operations
    let operationPromise = operationVersion
      .createWithResponseInfo<VoiceVerificationCheckResource>({
        uri: instance._uri,
        method: "post",
        headers,
      })
      .then(
        (response): ApiResponse<VoiceVerificationCheckInstance> => ({
          ...response,
          body: new VoiceVerificationCheckInstance(
            operationVersion,
            response.body
          ),
        })
      );

    operationPromise = instance._version.setPromiseCallback(
      operationPromise,
      callback
    );
    return operationPromise;
  };

  instance.toJSON = function toJSON() {
    return instance._solution;
  };

  instance[inspect.custom] = function inspectImpl(
    _depth: any,
    options: InspectOptions
  ) {
    return inspect(instance.toJSON(), options);
  };

  return instance;
}

interface VoiceVerificationCheckPayload
  extends VoiceVerificationCheckResource {}

interface VoiceVerificationCheckResource {
  to: string;
  callerIdSid: string;
}

export class VoiceVerificationCheckInstance {
  constructor(protected _version: V1, payload: VoiceVerificationCheckResource) {
    this.to = payload.to;
    this.callerIdSid = payload.callerIdSid;
  }

  /**
   * The phone number that was verified in E.164 format.
   */
  to: string;
  /**
   * The Caller ID SID created upon successful verification.
   */
  callerIdSid: string;

  /**
   * Provide a user-friendly representation
   *
   * @returns Object
   */
  toJSON() {
    return {
      to: this.to,
      callerIdSid: this.callerIdSid,
    };
  }

  [inspect.custom](_depth: any, options: InspectOptions) {
    return inspect(this.toJSON(), options);
  }
}
