{
  "dependencies": {
    "jest": "^29.4.3",
    "localtunnel": "^2.0.2",
    "twilio": "../../package"
  },
  "scripts": {
    "prepare": "tsc",
    "test": "jest lib --testPathIgnorePatterns=lib/webhook",
    "webhook-test": "jest --testPathPattern=lib/webhook"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "ts-jest": "^29.4.12",
    "typescript": "^7.0.2"
  }
}
